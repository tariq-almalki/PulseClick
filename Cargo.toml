[package]
name = "pulseclick"
version = "1.0.0"
edition = "2021"
description = "A small, responsive Windows auto-clicker"
license = "MIT"

[dependencies]
eframe = { version = "0.29", default-features = true }
image = { version = "0.25", default-features = false, features = ["png"] }

[build-dependencies]
winresource = "0.1"
