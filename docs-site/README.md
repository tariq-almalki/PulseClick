# PulseClick documentation

This is a local VitePress documentation site using the default VitePress theme.

```powershell
npm install
npm run docs:dev
```

The site is intentionally ready for local development first. The deployment notes in `docs/development.md` describe the small GitHub Pages change to make later.
